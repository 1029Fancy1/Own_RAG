下面是一份可以直接保存为 **`StudyMate_RAG_7天执行计划.md`** 的 Markdown 文件内容。它已经包含：**项目简介、技术栈、项目结构、7 天计划、每日耗时、核心功能范围、AI 辅助开发建议、最终交付物**。

```markdown
# StudyMate RAG：个人学习知识库问答系统 7 天执行计划

## 1. 项目简介

**StudyMate RAG** 是一个个人学习知识库问答系统。

用户可以上传自己的学习资料，例如：

- PDF 课程讲义
- Markdown 学习笔记
- TXT 文本文档
- 面试资料
- 技术文档
- 课程总结

系统会对上传资料进行解析、切分、向量化，并存入本地向量数据库。用户提问时，系统会先从知识库中检索相关内容，再结合大语言模型生成回答，并展示答案引用来源。

本项目的核心目标是完成一个可演示的 RAG 应用 MVP，而不是一开始就做成复杂商业产品。

---

## 2. 项目目标

本项目计划在 7 天内完成一个可运行、可演示、可写入简历的个人知识库问答系统。

最终系统应支持：

1. 上传 PDF / TXT / Markdown 文档
2. 自动解析文档内容
3. 将长文本切分为多个 chunk
4. 调用 Embedding 模型生成文本向量
5. 将文本向量存入 ChromaDB
6. 用户输入问题
7. 系统进行语义检索
8. 大模型基于检索结果生成回答
9. 展示回答引用的资料来源
10. 对知识库中没有的信息进行拒答，减少模型幻觉

---

## 3. 技术选型原则

本项目开发周期只有 7 天，核心目标是快速完成一个可演示的 RAG 项目。

因此，本项目采用 **纯 Python 技术栈 + Streamlit**，不使用 React / Next.js 作为前端。

原因如下：

- Streamlit 学习成本低，约 2 小时即可上手
- 不需要写 HTML / CSS / JavaScript
- 不需要前后端分离
- 不需要处理跨域、接口联调、复杂状态管理
- 更适合 AI 应用原型开发
- 能把主要时间投入到 RAG 核心流程上

本项目的重点不是复杂前端，而是：

- 文档解析
- 文本切分
- Embedding 向量化
- 向量数据库检索
- Prompt 设计
- 大模型回答
- 来源引用
- 拒答机制

---

## 4. 使用技术栈

| 模块 | 技术 | 作用 |
|---|---|---|
| Web 界面 | Streamlit | 构建上传页面、聊天界面和引用展示 |
| 编程语言 | Python | 项目主语言 |
| 文档解析 | PyMuPDF | 解析 PDF 文件 |
| 文本文件解析 | Python 原生文件读写 | 解析 TXT / Markdown |
| 文本切分 | Python 自定义切分函数 | 将长文本切分成 chunk |
| 向量数据库 | ChromaDB | 本地持久化存储文本向量 |
| Embedding 模型 | OpenAI / 通义千问 / DeepSeek / Ollama 可选 | 将文本转成向量 |
| 大语言模型 | OpenAI / 通义千问 / DeepSeek / Ollama 可选 | 根据上下文生成回答 |
| 环境变量管理 | python-dotenv | 管理 API Key 和模型配置 |
| 数据存储 | 本地文件 + ChromaDB | 保存上传文档和向量数据 |
| 项目管理 | pip / venv / conda | 管理 Python 依赖 |

---

## 5. 推荐最终技术组合

为了 7 天内稳定完成，推荐使用：

```text
Python
Streamlit
PyMuPDF
ChromaDB
OpenAI-compatible API
python-dotenv
```

如果使用 OpenAI：

```text
Embedding 模型：text-embedding-3-small
Chat 模型：gpt-4o-mini
```

如果使用国产模型或本地模型，可以替换为：

```text
通义千问
DeepSeek
智谱 GLM
Ollama
bge-small-zh
```

建议将 Embedding 和 Chat Model 封装成独立函数，方便后续替换模型。

---

## 6. 系统架构

整体流程如下：

```text
用户
 ↓
Streamlit 页面
 ↓
上传 PDF / TXT / MD
 ↓
文档解析 Document Loader
 ↓
文本切分 Text Splitter
 ↓
Embedding 模型
 ↓
ChromaDB 向量数据库
 ↓
用户提问
 ↓
问题向量化
 ↓
Retriever 语义检索
 ↓
组装 Prompt
 ↓
LLM 生成回答
 ↓
展示回答 + 引用来源
```

---

## 7. RAG 核心流程

本项目的 RAG Pipeline 如下：

```text
Document Upload
    ↓
Document Parsing
    ↓
Text Chunking
    ↓
Embedding
    ↓
Vector Store
    ↓
User Query
    ↓
Query Embedding
    ↓
Similarity Search
    ↓
Prompt Construction
    ↓
LLM Generation
    ↓
Answer with Sources
```

中文解释：

1. 用户上传学习资料
2. 系统解析文档内容
3. 将长文本切分成多个较小片段
4. 对每个片段生成 embedding 向量
5. 将文本片段和向量存入 ChromaDB
6. 用户输入问题
7. 系统将问题也转成向量
8. 在 ChromaDB 中检索最相关的文本片段
9. 将相关片段和用户问题拼成 Prompt
10. 调用大模型生成回答
11. 返回回答和引用来源

---

## 8. 项目目录结构

推荐目录结构如下：

```text
studymate-rag/
├── app.py
├── requirements.txt
├── .env
├── .env.example
├── .gitignore
├── README.md
├── data/
│   ├── uploads/
│   └── chroma/
├── src/
│   ├── __init__.py
│   ├── config.py
│   ├── document_loader.py
│   ├── text_splitter.py
│   ├── embedding_service.py
│   ├── vector_store.py
│   ├── llm_service.py
│   ├── rag_pipeline.py
│   └── file_utils.py
└── examples/
    ├── sample_note.md
    └── sample_text.txt
```

---

## 9. 文件职责说明

| 文件 / 目录                | 职责                                       |
| -------------------------- | ------------------------------------------ |
| `app.py`                   | Streamlit 页面入口，负责界面展示和用户交互 |
| `requirements.txt`         | 记录项目依赖                               |
| `.env`                     | 保存本地 API Key，不提交到 GitHub          |
| `.env.example`             | 环境变量示例文件，可提交到 GitHub          |
| `.gitignore`               | 忽略缓存、环境变量、本地数据等文件         |
| `README.md`                | 项目说明文档                               |
| `data/uploads/`            | 保存用户上传的原始文档                     |
| `data/chroma/`             | 保存 ChromaDB 本地向量数据                 |
| `src/config.py`            | 读取环境变量和全局配置                     |
| `src/document_loader.py`   | 解析 PDF / TXT / Markdown 文档             |
| `src/text_splitter.py`     | 将长文本切分为 chunk                       |
| `src/embedding_service.py` | 调用 Embedding 模型生成向量                |
| `src/vector_store.py`      | 管理 ChromaDB 的写入、检索和清空           |
| `src/llm_service.py`       | 调用大语言模型生成回答                     |
| `src/rag_pipeline.py`      | 串联检索、Prompt 构造和答案生成流程        |
| `src/file_utils.py`        | 文件保存、路径处理、清空目录等工具函数     |
| `examples/`                | 存放测试用学习资料                         |

---

## 10. 7 天开发计划总览

| 天数  | 主题                   | 核心任务                                       | 预计耗时   |
| ----- | ---------------------- | ---------------------------------------------- | ---------- |
| Day 1 | 项目初始化             | 创建项目、安装依赖、搭建 Streamlit 页面骨架    | 2.5–4 小时 |
| Day 2 | 文档上传与解析         | 上传 PDF/TXT/MD，保存文件并解析文本            | 3–5 小时   |
| Day 3 | 文本切分与元数据       | 实现 chunk 切分，保留文档来源信息              | 3–5 小时   |
| Day 4 | Embedding 与向量入库   | 生成向量，写入 ChromaDB，测试检索              | 4–6 小时   |
| Day 5 | RAG 问答链路           | 检索相关片段，构造 Prompt，调用 LLM 回答       | 4–6 小时   |
| Day 6 | 质量优化与 UI 完善     | 增加拒答机制、引用来源、清空知识库和页面优化   | 3–5 小时   |
| Day 7 | 测试、修 Bug、整理展示 | 全流程测试、录 Demo、完善 README、准备简历描述 | 3–5 小时   |

总体预计：

```text
有 Python 基础：22–31 小时
新手：30–40 小时
熟悉 AI 辅助开发：20–25 小时
```

---

# 11. 每日详细计划

---

## Day 1：项目初始化 + Streamlit 页面骨架

### 今日目标

完成项目基础搭建，让 Streamlit 页面能够运行。

### 预计耗时

```text
2.5–4 小时
```

### 具体任务

1. 创建项目目录

```bash
mkdir studymate-rag
cd studymate-rag
```

2. 创建虚拟环境

```bash
python -m venv .venv
```

Windows 激活：

```bash
.venv\Scripts\activate
```

macOS / Linux 激活：

```bash
source .venv/bin/activate
```

3. 安装基础依赖

```bash
pip install streamlit python-dotenv pymupdf chromadb openai
```

4. 创建 `requirements.txt`

```bash
pip freeze > requirements.txt
```

5. 创建基础目录

```text
data/uploads/
data/chroma/
src/
examples/
```

6. 创建 Streamlit 页面骨架

`app.py` 示例：

```python
import streamlit as st

st.set_page_config(
    page_title="StudyMate RAG",
    page_icon="📚",
    layout="wide"
)

st.title("📚 StudyMate RAG")
st.caption("上传学习资料，构建你的个人 AI 学习助手")

with st.sidebar:
    st.header("知识库")
    uploaded_files = st.file_uploader(
        "上传 PDF / TXT / MD 文件",
        type=["pdf", "txt", "md"],
        accept_multiple_files=True
    )

st.subheader("开始提问")

question = st.chat_input("请输入你想问的问题...")

if question:
    with st.chat_message("user"):
        st.write(question)

    with st.chat_message("assistant"):
        st.write("这里将显示基于知识库生成的回答。")
```

7. 运行项目

```bash
streamlit run app.py
```

### 今日交付物

- 项目目录创建完成
- 虚拟环境创建完成
- Streamlit 页面可以运行
- 页面包含上传入口和聊天输入框
- README 写好项目简介

---

## Day 2：文档上传与解析

### 今日目标

实现文档上传、文件保存和文本解析。

### 预计耗时

```text
3–5 小时
```

### 具体任务

1. 实现文件保存功能

`src/file_utils.py`：

```python
from pathlib import Path

UPLOAD_DIR = Path("data/uploads")
UPLOAD_DIR.mkdir(parents=True, exist_ok=True)

def save_uploaded_file(uploaded_file):
    file_path = UPLOAD_DIR / uploaded_file.name

    with open(file_path, "wb") as f:
        f.write(uploaded_file.getbuffer())

    return str(file_path)
```

2. 实现 PDF 解析

`src/document_loader.py`：

```python
import fitz

def load_pdf(file_path: str) -> str:
    doc = fitz.open(file_path)
    texts = []

    for page_num, page in enumerate(doc, start=1):
        text = page.get_text()
        if text.strip():
            texts.append(f"[Page {page_num}]\n{text}")

    return "\n\n".join(texts)
```

3. 实现 TXT / MD 解析

```python
def load_text_file(file_path: str) -> str:
    with open(file_path, "r", encoding="utf-8") as f:
        return f.read()
```

4. 统一文档加载函数

```python
def load_document(file_path: str) -> str:
    suffix = file_path.lower().split(".")[-1]

    if suffix == "pdf":
        return load_pdf(file_path)
    elif suffix in ["txt", "md"]:
        return load_text_file(file_path)
    else:
        raise ValueError("Unsupported file type")
```

5. 在 Streamlit 页面显示解析预览

```python
with st.expander("查看解析文本预览"):
    st.write(text[:2000])
```

### 今日交付物

- 可以上传 PDF / TXT / MD
- 上传文件保存到 `data/uploads/`
- 文档可以解析成纯文本
- 页面可以预览解析结果

---

## Day 3：文本切分与元数据设计

### 今日目标

将解析后的长文本切分成多个 chunk，并保留来源信息。

### 预计耗时

```text
3–5 小时
```

### 推荐参数

```text
chunk_size = 800
overlap = 120
```

### 具体任务

1. 实现文本切分函数

`src/text_splitter.py`：

```python
def split_text(text: str, chunk_size: int = 800, overlap: int = 120):
    chunks = []
    start = 0
    chunk_id = 0

    while start < len(text):
        end = start + chunk_size
        chunk = text[start:end].strip()

        if chunk:
            chunks.append({
                "chunk_id": chunk_id,
                "text": chunk
            })
            chunk_id += 1

        start += chunk_size - overlap

    return chunks
```

2. 构造带 metadata 的 chunks

```python
def build_chunks(text: str, filename: str):
    raw_chunks = split_text(text)

    chunks = []
    for item in raw_chunks:
        chunks.append({
            "id": f"{filename}_{item['chunk_id']}",
            "text": item["text"],
            "metadata": {
                "filename": filename,
                "chunk_id": item["chunk_id"]
            }
        })

    return chunks
```

3. 页面展示切分结果

```text
文档名称：xxx.pdf
切分数量：25 chunks
```

### 今日交付物

- 文本可以切分为多个 chunk
- 每个 chunk 有唯一 ID
- 每个 chunk 保留 filename 和 chunk_id
- 页面可以显示切分数量

---

## Day 4：Embedding 与 ChromaDB 入库

### 今日目标

实现文本向量化，并将 chunks 存入 ChromaDB。

### 预计耗时

```text
4–6 小时
```

### 具体任务

1. 配置环境变量

`.env`：

```text
OPENAI_API_KEY=你的 API Key
CHAT_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small
```

`.env.example`：

```text
OPENAI_API_KEY=
CHAT_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small
```

2. 读取配置

`src/config.py`：

```python
import os
from dotenv import load_dotenv

load_dotenv()

OPENAI_API_KEY = os.getenv("OPENAI_API_KEY")
CHAT_MODEL = os.getenv("CHAT_MODEL", "gpt-4o-mini")
EMBEDDING_MODEL = os.getenv("EMBEDDING_MODEL", "text-embedding-3-small")
```

3. 实现 Embedding 服务

`src/embedding_service.py`：

```python
from openai import OpenAI
from src.config import OPENAI_API_KEY, EMBEDDING_MODEL

client = OpenAI(api_key=OPENAI_API_KEY)

def get_embedding(text: str):
    response = client.embeddings.create(
        model=EMBEDDING_MODEL,
        input=text
    )

    return response.data[0].embedding
```

4. 初始化 ChromaDB

`src/vector_store.py`：

```python
import chromadb

def get_chroma_collection():
    client = chromadb.PersistentClient(path="data/chroma")
    collection = client.get_or_create_collection(name="studymate")
    return collection
```

5. 写入 chunks

```python
def add_chunks_to_chroma(collection, chunks, embedding_function):
    ids = []
    documents = []
    metadatas = []
    embeddings = []

    for chunk in chunks:
        ids.append(chunk["id"])
        documents.append(chunk["text"])
        metadatas.append(chunk["metadata"])
        embeddings.append(embedding_function(chunk["text"]))

    collection.add(
        ids=ids,
        documents=documents,
        metadatas=metadatas,
        embeddings=embeddings
    )
```

6. 实现检索函数

```python
def search(collection, query: str, embedding_function, top_k: int = 5):
    query_embedding = embedding_function(query)

    results = collection.query(
        query_embeddings=[query_embedding],
        n_results=top_k
    )

    return results
```

### 今日交付物

- 可以生成文本 embedding
- chunks 可以写入 ChromaDB
- 可以根据问题检索相关 chunks
- `data/chroma/` 中出现持久化数据

---

## Day 5：RAG 问答链路打通

### 今日目标

实现完整问答流程：用户提问 → 检索 → 构造 Prompt → LLM 回答 → 显示来源。

### 预计耗时

```text
4–6 小时
```

### 具体任务

1. 构造 Prompt

`src/rag_pipeline.py`：

```python
def build_prompt(question: str, contexts: list[str]) -> str:
    context_text = "\n\n".join(
        [f"资料片段 {i + 1}：\n{ctx}" for i, ctx in enumerate(contexts)]
    )

    return f"""
你是 StudyMate，一个严谨、清晰的学习助手。

请只根据下面的资料片段回答用户问题。
如果资料中没有足够信息，请回答：
“当前知识库中没有足够信息回答这个问题。”

回答要求：
1. 不要编造资料中不存在的信息。
2. 尽量用分点方式解释。
3. 如果涉及定义、步骤或对比，请表达清楚。
4. 回答末尾给出你参考了哪些资料片段。

资料片段：
{context_text}

用户问题：
{question}

请给出回答：
"""
```

2. 实现 LLM 调用

`src/llm_service.py`：

```python
from openai import OpenAI
from src.config import OPENAI_API_KEY, CHAT_MODEL

client = OpenAI(api_key=OPENAI_API_KEY)

def generate_answer(prompt: str):
    response = client.chat.completions.create(
        model=CHAT_MODEL,
        messages=[
            {"role": "system", "content": "你是一个严谨的学习助手。"},
            {"role": "user", "content": prompt}
        ],
        temperature=0.2
    )

    return response.choices[0].message.content
```

3. 串联 RAG Pipeline

```python
from src.embedding_service import get_embedding
from src.vector_store import search
from src.llm_service import generate_answer

def answer_question(collection, question: str, top_k: int = 5):
    results = search(collection, question, get_embedding, top_k=top_k)

    contexts = results["documents"][0]
    metadatas = results["metadatas"][0]
    distances = results.get("distances", [[]])[0]

    prompt = build_prompt(question, contexts)
    answer = generate_answer(prompt)

    return {
        "answer": answer,
        "contexts": contexts,
        "metadatas": metadatas,
        "distances": distances
    }
```

4. 在 Streamlit 页面展示回答

```python
if "messages" not in st.session_state:
    st.session_state.messages = []

question = st.chat_input("请输入你的问题")

if question:
    st.session_state.messages.append({
        "role": "user",
        "content": question
    })

    with st.chat_message("user"):
        st.write(question)

    with st.chat_message("assistant"):
        with st.spinner("StudyMate 正在查阅你的资料..."):
            result = answer_question(collection, question, top_k=5)
            st.write(result["answer"])

            with st.expander("查看引用来源"):
                for i, ctx in enumerate(result["contexts"]):
                    st.markdown(f"**来源 {i + 1}**")
                    st.write(ctx[:1000])
```

### 今日交付物

- 用户可以提问
- 系统可以检索知识库
- 系统可以调用大模型回答
- 页面可以展示答案
- 页面可以展示引用来源

---

## Day 6：质量优化与 UI 完善

### 今日目标

提升项目可用性和演示效果。

### 预计耗时

```text
3–5 小时
```

### 具体任务

1. 增加无关问题拒答机制

```python
def is_relevant(results, max_distance=1.2):
    distances = results.get("distances", [[]])[0]

    if not distances:
        return False

    best_distance = min(distances)
    return best_distance <= max_distance
```

如果不相关，返回：

```text
当前知识库中没有足够信息回答这个问题。
```

2. 优化引用来源展示

展示内容包括：

```text
来源编号
文件名
chunk_id
距离分数
文本片段
```

3. 增加知识库状态展示

在侧边栏显示：

```text
已上传文件数量
当前 top_k
当前 chunk_size
知识库路径
```

4. 增加清空知识库功能

```python
import shutil
from pathlib import Path

def clear_knowledge_base():
    chroma_path = Path("data/chroma")
    upload_path = Path("data/uploads")

    if chroma_path.exists():
        shutil.rmtree(chroma_path)

    if upload_path.exists():
        shutil.rmtree(upload_path)

    chroma_path.mkdir(parents=True, exist_ok=True)
    upload_path.mkdir(parents=True, exist_ok=True)
```

5. 优化 Streamlit 页面布局

推荐布局：

```text
左侧 Sidebar：
- 项目名称
- 文件上传
- 参数设置
- 知识库状态
- 清空知识库按钮

主区域：
- 项目说明
- 聊天窗口
- 回答内容
- 引用来源折叠卡片
```

### 今日交付物

- 能处理知识库无答案的问题
- 引用来源显示更清晰
- UI 更完整
- 支持清空知识库
- 支持参数设置，例如 top_k

---

## Day 7：测试、修 Bug、录 Demo、整理 README

### 今日目标

完成全流程测试，修复问题，准备展示材料。

### 预计耗时

```text
3–5 小时
```

### 测试清单

| 测试项       | 预期结果                      |
| ------------ | ----------------------------- |
| 上传 PDF     | 能保存并解析                  |
| 上传 TXT     | 能保存并解析                  |
| 上传 MD      | 能保存并解析                  |
| 空知识库提问 | 提示先上传资料                |
| 资料内问题   | 能基于资料回答                |
| 资料外问题   | 不胡编，返回信息不足          |
| 引用来源     | 能显示文件名、chunk_id 和片段 |
| 刷新页面     | ChromaDB 数据仍存在           |
| 清空知识库   | 本地数据被清除                |
| API Key 缺失 | 页面给出明确提示              |

### Demo 演示流程

```text
1. 打开 StudyMate RAG
2. 上传一份 PDF 或 Markdown 学习资料
3. 系统显示解析和索引成功
4. 提问一个资料内问题
5. 展示回答和引用来源
6. 提问一个资料外问题
7. 展示系统拒答
8. 展示 README 和项目结构
```

### README 需要包含

```text
项目简介
功能特性
技术栈
系统架构
RAG 流程
项目结构
本地运行方式
环境变量配置
测试示例
项目截图
后续优化方向
```

### 今日交付物

- 项目稳定可运行
- README 完整
- GitHub 仓库整理完成
- Demo 视频或截图准备完成
- 简历描述准备完成

---

## 12. 必做功能范围

7 天内必须完成以下功能：

- 上传 PDF / TXT / Markdown
- 文档解析
- 文本切分
- Embedding 向量化
- ChromaDB 持久化存储
- 用户提问
- 语义检索
- Prompt 构造
- LLM 回答
- 来源引用
- 无关问题拒答
- 清空知识库
- README 文档
- Demo 演示

---

## 13. 可选功能

如果时间充裕，可以考虑加入：

- 多文件批量上传
- 文档列表展示
- 按文档筛选问答
- 聊天历史导出
- 自定义 chunk_size
- 自定义 overlap
- 自定义 top_k
- 支持 Ollama 本地模型
- 支持国产 Embedding 模型
- 支持 OCR 解析扫描版 PDF
- Docker 部署

---

## 14. 暂时不做的功能

为了保证 7 天内完成，第一版不做以下功能：

- React / Next.js 前端
- 用户登录
- 多用户隔离
- 云端部署
- 权限系统
- 知识图谱
- Agent 工具调用
- WebSocket
- 异步任务队列
- 超大文件处理
- 在线协作

---

## 15. AI 辅助开发建议

可以使用 AI 生成代码，但不要一次性让 AI 生成整个项目。

推荐方式是按模块生成：

### 适合让 AI 生成的模块

```text
src/document_loader.py
src/text_splitter.py
src/embedding_service.py
src/vector_store.py
src/llm_service.py
src/rag_pipeline.py
app.py 的部分页面逻辑
README.md
```

### 推荐提问方式

```text
请帮我写一个 src/document_loader.py，支持解析 PDF、TXT、Markdown，返回纯文本。
```

```text
请帮我写一个 src/vector_store.py，使用 ChromaDB 持久化保存 chunks，并提供 add_chunks 和 search 两个函数。
```

```text
请帮我写一个 Streamlit app.py，左侧上传文档，右侧聊天问答，调用已有的 rag_pipeline.py。
```

```text
我运行时报错如下：[粘贴完整错误]
这是我的目录结构：[粘贴目录结构]
请帮我定位问题并给出修改方案。
```

### 不推荐的提问方式

```text
请帮我生成完整的 StudyMate RAG 项目。
```

原因：

- 代码容易混乱
- 文件之间依赖不清晰
- 很多细节跑不起来
- 不利于理解和调试

---

## 16. 开发过程中的常见坑

### 1. 上传成功但没有索引

必须确保执行完整流程：

```text
上传文件 → 解析文本 → 切分 chunk → 生成 embedding → 写入 ChromaDB
```

### 2. PDF 解析为空

可能原因：

- PDF 是扫描版
- PDF 是图片，没有文本层
- 文件加密或格式异常

第一版可以暂时不支持 OCR。

### 3. chunk 太大或太小

推荐起始参数：

```text
chunk_size = 800
overlap = 120
top_k = 5
```

### 4. 模型胡编答案

解决方法：

- Prompt 中要求只根据资料回答
- 展示引用来源
- 增加相似度阈值
- 无关问题直接拒答

### 5. ChromaDB 重复写入

如果同一个文件重复上传，可能会出现重复 ID 或重复内容。

解决方向：

- 文件名加时间戳
- chunk id 使用 hash
- 上传前检查是否已存在
- 提供清空知识库按钮

### 6. API Key 泄露

`.env` 不能提交到 GitHub。

`.gitignore` 应包含：

```text
.env
.venv/
__pycache__/
data/chroma/
data/uploads/
```

---

## 17. 推荐 `.gitignore`

```gitignore
# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environment
.venv/
venv/
env/

# Environment variables
.env

# Streamlit
.streamlit/secrets.toml

# Local data
data/uploads/
data/chroma/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
```

---

## 18. 推荐 `requirements.txt`

初始版本可以包含：

```text
streamlit
python-dotenv
pymupdf
chromadb
openai
```

如果后续使用 LangChain，可增加：

```text
langchain
langchain-openai
langchain-community
```

但第一版不强制使用 LangChain。

---

## 19. 简历描述

可以在简历中这样写：

```text
StudyMate RAG｜个人学习知识库问答系统

- 使用 Streamlit、ChromaDB 和大语言模型构建个人学习资料问答系统，支持 PDF、TXT、Markdown 文档上传与解析。
- 实现文档切分、Embedding 向量化、ChromaDB 持久化存储、语义检索和基于上下文的答案生成。
- 设计来源引用和低相关度拒答机制，减少大模型幻觉，提高回答可追溯性。
- 使用 Streamlit 快速构建交互式聊天界面，实现知识库状态展示、引用片段查看和本地知识库管理。
```

技术化版本：

```text
- 设计并实现 RAG Pipeline：Document Loader → Text Splitter → Embedding → Vector Store → Retriever → Prompt Builder → LLM Generator。
- 使用 ChromaDB 存储文档向量，通过 top-k 语义检索召回相关上下文，并结合 Prompt Engineering 约束模型基于资料回答。
```

---

## 20. 最终交付物

7 天结束时，应完成以下交付物：

```text
1. 可运行的 Streamlit 应用
2. 支持上传和解析 PDF / TXT / MD
3. 支持文档切分和向量化
4. 支持 ChromaDB 本地持久化存储
5. 支持基于资料的问答
6. 支持引用来源展示
7. 支持无关问题拒答
8. 支持清空知识库
9. 完整 README
10. Demo 截图或录屏
11. 可提交 GitHub 的项目代码
12. 可写入简历的项目描述
```

---

## 21. 最终开发策略

本项目最重要的是先打通核心链路：

```text
上传文档
 ↓
解析文本
 ↓
切分 chunk
 ↓
生成 embedding
 ↓
写入 ChromaDB
 ↓
用户提问
 ↓
检索相关片段
 ↓
构造 Prompt
 ↓
LLM 生成回答
 ↓
展示答案和引用来源
```

不要一开始追求复杂功能。

推荐节奏：

```text
前 2 天：完成文档上传和解析
第 3–4 天：完成切分、向量化和检索
第 5 天：完成 RAG 问答
第 6 天：优化质量和界面
第 7 天：测试、修复、整理展示
```

项目第一版的目标是：

```text
稳定、清晰、可演示、能讲明白技术原理。
```

而不是：

```text
功能很多但跑不稳。
```

---

## 22. 一句话总结

StudyMate RAG 的第一版应该采用：

```text
Python + Streamlit + PyMuPDF + ChromaDB + Embedding Model + LLM
```

用最短路径完成一个完整 RAG 应用，把精力集中在：

```text
文档处理、向量检索、上下文增强生成、来源引用和拒答机制
```

这比在 7 天内额外学习 React / Next.js 更稳、更高效，也更适合作为个人 AI 项目展示。
```

直接把上面内容复制到本地文件：

```text
StudyMate_RAG_7天执行计划.md
```

就可以作为你的项目执行文档使用。