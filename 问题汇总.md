# 问题汇总

StudyMate RAG 项目开发过程中遇到的 Bug 与问题记录，便于回顾学习。

---

## 问题 #1: `AttributeError: 'str' object has no attribute 'mkdir'`

**日期**: 2026-05-26  
**阶段**: Day 2 — 文档上传与解析  
**严重度**: 🔴 阻塞（页面无法启动）

### 现象

```
AttributeError: 'str' object has no attribute 'mkdir'
File "D:\studymate-rag\src\file_utils.py", line 20, in <module>
    UPLOAD_DIR.mkdir(parents=True, exist_ok=True)
    ^^^^^^^^^^^^^^^^
```

### 调用链

```
streamlit run app.py
  → app.py:15 from src.file_utils import ...
    → file_utils.py:14 from src.config import UPLOAD_DIR
      → file_utils.py:20 UPLOAD_DIR.mkdir(...)  ← 💥 炸在这里
```

### 根因

`config.py` 中用 `os.path.join()` 生成的路径常量是 `str` 类型，而 `file_utils.py` 中调用了 `Path` 对象才有的 `.mkdir()` 方法和 `/` 运算符。

```python
# config.py — 问题代码
import os
BASE_DIR: str = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DATA_DIR: str = os.path.join(BASE_DIR, "data")
UPLOAD_DIR: str = os.path.join(DATA_DIR, "uploads")   # ← str 类型
```

### 问题性质

**类型不匹配（Type Mismatch）/ API 边界污点**：数据在一个模块产生时是 `str`，传到另一个模块时按 `Path` 的方法调用，中间没有显式转换。

### 修复

将 `config.py` 中 4 个路径常量从 `os.path` 改为 `pathlib.Path` 写法：

```python
# config.py — 修复后
from pathlib import Path

BASE_DIR: Path = Path(__file__).resolve().parent.parent
DATA_DIR: Path = BASE_DIR / "data"
UPLOAD_DIR: Path = DATA_DIR / "uploads"
CHROMA_DIR: Path = DATA_DIR / "chroma"
```

### 关键经验

1. **修源头，不修末端**：bug 报在 `file_utils.py`，但根在 `config.py`。如果在 `file_utils.py` 包一层 `Path(UPLOAD_DIR)`，每个引用处都要改，治标不治本。
2. **`os.path.join` → `str`，`Path /` → `Path`**：记住这两种 API 的返回类型，混用必然出错。
3. **Traceback 往上追**：报错位置不一定是 bug 源头，沿着调用链往上找。
4. **类型注解不只是装饰**：`UPLOAD_DIR: str` 写了却没起到警示作用 — 如果当初写 `UPLOAD_DIR: Path`，IDE 就会在 `os.path.join(...)` 处标红，提前拦截。

### 涉及文件

| 文件 | 角色 |
|------|------|
| `src/config.py` | 根源 — `os.path.join` 返回 `str` |
| `src/file_utils.py` | 引爆点 — 对 `str` 调用了 `Path` 方法 |

---

## 问题 #2: 缩进错误导致 `start += step` 在条件分支内 → 死循环

**日期**: 2026-05-28  
**阶段**: Day 3 — text_splitter.py `split_text` 函数  
**严重度**: 🔴 阻塞（页面卡死）

### 现象

上传文档后点击"解析并保存"，页面无限转圈，Streamlit 无响应。

### 根因

`start += step` 写在 `if chunk_text:` 块内部。如果某个 chunk 的文本 strip 后为空字符串（原文中间的大段空白行），`if` 条件不成立，`start` 永远不会推进，while 循环变死循环。

```python
# 错误写法
if chunk_text:
    chunks.append(...)
    chunk_id += 1
    start += step  # ← 空 chunk 时不会执行，窗口卡在原地

# 正确写法
if chunk_text:
    chunks.append(...)
    chunk_id += 1
start += step  # ← 无论 chunk 是否为空，每次迭代窗口都必须推进
```

### 问题性质

**缩进层级错误**：一条必须每次迭代都执行的语句被错误地放进了条件分支内。

### 关键经验

1. **循环推进语句不应写在条件分支里**：while 的迭代器更新（`start += step`）必须保证每轮都执行。
2. **边界场景要考虑空数据**：文本中间可能存在大段空白，`.strip()` 后可能为空。
3. **Python 缩进 = 作用域**：多一格少一格，语义完全不同。

---

## 问题 #3: `result.append(chunk)` 在 for 循环外 → 只返回最后 1 个 chunk

**日期**: 2026-05-28  
**阶段**: Day 3 — text_splitter.py `build_chunks` 函数  
**严重度**: 🔴 数据丢失

### 现象

25 个 chunk 的 PDF 解析后 `parsed_docs` 只有 1 个 chunk。

### 根因

`result.append(chunk)` 缩进与 `for` 同级，在循环体外执行。每次迭代覆盖 `chunk` 变量，循环结束后只 append 最后那个。

```python
# 错误写法
for item in raw_chunks:
    chunk = {...}
result.append(chunk)  # ← 和 for 同级，循环结束后只执行一次

# 正确写法
for item in raw_chunks:
    chunk = {...}
    result.append(chunk)  # ← 在循环体内，每轮都执行
```

### 关键经验

同问题 #2 — Python 缩进就是作用域，写完循环先用 `len()` 快速验证。

---

## 知识点: ChromaDB 中 chunk 为什么需要唯一 id

**日期**: 2026-05-28  
**阶段**: Day 3 — 元数据设计  
**性质**: 设计决策

### 问题

为什么 chunk id 不能只用 `chunk_id`（0, 1, 2...），而要用 `"{filename}_{chunk_id}"`？

### 解释

ChromaDB 的 `collection.add()` 行为是 **upsert**（id 存在则覆盖，不存在则新增）。如果 `笔记.pdf` 和 `讲义.pdf` 各产生 3 个 chunk，都用 0/1/2 当 id：

```
笔记.pdf 写入 → ids: [0, 1, 2]  ✅
讲义.pdf 写入 → ids: [0, 1, 2]  💥 覆盖了笔记.pdf 的数据
```

加上文件名前缀后：`笔记.pdf_0` / `讲义.pdf_0`，所有 chunk 各占各的位置，互不影响。

### 类比

图书馆索书号 = 分类号 + 序号。只用序号编目，不同作者的书就混了。ChromaDB 的 id 就是索书号。

### 进阶

生产环境用 hash 作 id：`hashlib.md5(text.encode()).hexdigest()` — 相同内容的 chunk 无论何时上传 id 都一样，天然支持去重。

---
