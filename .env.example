# StudyMate RAG 环境变量配置
# 复制此文件为 .env 并填入你的实际值

# OpenAI 兼容 API 密钥（必填）
OPENAI_API_KEY=sk-your-api-key-here

# 对话模型名称（可选，默认 gpt-4o-mini）
CHAT_MODEL=gpt-4o-mini

# 嵌入模型名称（可选，默认 text-embedding-3-small）
EMBEDDING_MODEL=text-embedding-3-small
